name: game_life_flutter_release
description: "Game Life — evolução pessoal gamificada local-first para campanha, compromisso e ritmo diário."
publish_to: 'none'
version: 4.5.1+58

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.3
  path: ^1.9.1
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.4.2

flutter:
  uses-material-design: true
  assets:
    - assets/icons/game_life_icon.png
